# TorontoKnifeShow

AVIF files are generated with magick mogrify
`magick mogrify -format avif *.png`
This converts all images of the current folder


To Do
- convert table directory into text on website, image is not accessible